return {
    command = 'extramenu', -- Command to open the extramenu.
    commandEnabled = true, -- Set to true if the command should be enabled, false to disable it.

    keybindEnabled = true, -- Set to true if you want to use a keybind to open the extramenu.
    keybindKey = 'F2' -- The key to use for the keybind
}
